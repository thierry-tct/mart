# Replace Only operator classes

This directory contain:
    - The base abstract class implemented by each mutation operator Replace Only (not expected to be matched). Example: mutating `e` into `abs(e)`, or `Stmt` into `del Stmt`, _a_ and _b_ are numeric values (integer or floating point). 
    - The directory operator contains the mutation operators implementin this base class.
