# Numeric Variable operator classes

This directory contain:
    - The base abstract class implemented by the mutation operator for variable. Example: mutating `x+1` into `(x/3)+1`, where _x_ is a numeric value (integer of floating point) or pointer. 
    - The directory operator contains the mutation operators implementing this base class.
