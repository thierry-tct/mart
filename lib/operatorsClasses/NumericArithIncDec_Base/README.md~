# Numeric Variable Arithmetic Increment and Decrement operator classes

This directory contain:
    - The base abstract class implemented by each mutation operator for numeric variable arithmetic Increment and Decrementt. Example: mutating `v++` into `--v`, where _x_ is a numeric value (integer of floating point). 
    - The directory operator contains the mutation operators implementin this base class.
